/**
 * Grouped work models for supersets, circuits, and giant sets.
 * Based on TRAINING_MODEL.md and design.md specifications.
 */
export enum GroupType {
  SUPERSET = 'SUPERSET',
  TRI_SET = 'TRI_SET',
  GIANT_SET = 'GIANT_SET',
  CIRCUIT = 'CIRCUIT',
}
